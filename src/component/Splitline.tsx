import { FC } from 'react';

const Splitline: FC = () => {
  return (
    <div
      style={{ width: '100%', background: 'black', height: 2, marginTop: 16, marginBottom: 16 }}
    />
  );
};

export default Splitline;
